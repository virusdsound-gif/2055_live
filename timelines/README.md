# Timelines

Archive labels from Termux. Not a runtime. Not an activation.

`freq-0.7.json` names the root. The other Hz files (99.9, 200, 369) are map residue. `paradox_density` and `morning_star_active` in old dumps were theatre.

The sitting does not read this folder.
